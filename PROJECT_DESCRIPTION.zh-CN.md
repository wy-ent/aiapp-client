# PROJECT_DESCRIPTION.md — 项目描述（供人类与 AI 协作助手阅读）

> 本文件用来让**任何 AI 软件 / 开发者贡献者**无需访问飞书文档即可了解本项目全貌。
> 与其配套的其他文档见文末「文档索引」。本文会持续迭代，请保持与代码实现同步。

---

## 1. 项目一句话

**aiapp-mb ｜ OS 系统级的超级 AI 原生应用平台**：以操作系统级别的架构承载 AI 原生应用生态——用户用自然语言一键生成应用，一次生成、多处运行，覆盖 Web、安卓、iOS、鸿蒙、Windows、macOS、车机、电视盒等全平台，生成产物为统一格式的应用包 `.aiapp`，平台核心可独立部署，最终演进为可离线运行 AI 模型、支持去中心化分发的 AI 原生操作系统。

```
自然语言描述 → aiapp-gen 生成 MoonBit 工程 → aiapp-build 编译 → .aiapp 统一应用包（Web/多端容器运行）
```

核心开发语言：**MoonBit**（解析中间描述、生成前端代码、构造统一 app 包）；宿主容器侧后续以 Rust 实现。

## 2. 统一应用包格式 `.aiapp`

- WASM 字节码（业务逻辑）+ 容器应用接口逻辑（应用与宿主运行时的接口协议）
- 关键设计：`aiapp.json`（清单，描述 app_id / name / version / 权限等）+ `main.wasm`
- 运行时与格式开源做生态与信任，生成技能与运营数据闭源变现（见 §4）

## 3. 现状功能（模拟单用户原型）

- **应用市场首页**：模板缩略图/预览展示、标签分类、平台筛选、「当前平台不支持」备选、点击应用按「模拟打开 App」风格交互（最小化后台 / 关闭返回首页）
- **生成器**：新应用 / 基于某应用参考新建 / 更新已有应用（更新后需所有者同意才发布）；生成技能提示词可后台在线编辑迭代
- **后台管理系统**：应用审核（通过/驳回+意见）、上架、停用、恢复、编辑、删除；用户管理与启停；使用统计（应用总数、启动次数、注册/活跃用户、举报、激励预留）；生成提示词查看/编辑/恢复
- **举报机制**：应用详情可填写原因举报并落库，后台可见

组件：`crates/aiapp-web`(前端市场+后台) / `aiapp-gen`(社区版生成) / `aiapp-build`(编译打包) / `aiapp-cli`(CLI)。

## 4. 商业化架构：开源社区版 + 闭源 Pro

> 核心原则：**前期就把"可开源"与"必须闭源"分开**，物理隔离成独立仓库，后续才能放心开源引生态、闭源收钱（激励 / 广告分成 / 计费）。类比 AOSP（开源）+ GMS（闭源）。

### 4.1 两张部署面（后台系统的划分）
- **企业本地后台（开源社区版）**：企业自建部署，管理**本企业**的成员与本地应用市场。
- **全局运营后台（闭源 Pro / SaaS）**：运营方统一做**跨企业**统计、用户激励、广告分成、计费授权 —— 即"收费站"。

### 4.2 开源（开放仓库，MIT / Apache 2.0）
- `.aiapp` 应用包格式规范、运行时引擎 / Player、开发者工具 `aiapp-cli`、MoonBit 示例应用
- 企业自托管服务 + 企业本地后台（单租户管理本企业用户/应用/审核）
- 生成能力**接口契约**（只开接口，不开实现）
- 社区离线生成：Mock / 模板（`aiapp-gen` 社区版）、`aiapp-web`、`aiapp-build`、`docs`

### 4.3 闭源（私有仓 `pro/`，核心商业价值）
- 真实生成引擎 `aiapp-gen-pro`：LLM 生成引擎 + **可迭代提示词 DNA**（带版本、结构化分区，后台在线编辑持续迭代、对生成立即生效）
- 全局运营后台 `aiapp-saas`（规划）：跨企业统计、激励 / 广告分成、计费授权
- 官方精品应用市场、模型 key、内网地址、Prompt DNA —— 一律密闭

> 一条铁律：**运行时 + 格式开源做生态与信任；生成技能 + 运营数据闭源变现。**

### 4.4 仓库 / 目录映射
当前单仓为开源仓；闭源统一放 `pro/`（已在 `.gitignore`，可 `cd pro && git init` 单独推送私有仓）。

```
当前开源仓
├─ crates
│  ├─ aiapp-format/      (规划) 应用包格式    → 开源
│  ├─ aiapp-engine/      (规划) 运行时引擎    → 开源
│  ├─ aiapp-cli/                开发者工具    → 开源
│  ├─ aiapp-web/                市场 + 企业后台 → 开源(社区版)
│  ├─ aiapp-build/               构建打包      → 开源
│  └─ aiapp-gen/                生成(社区)      → 开源（仅 Mock/模板）
└─ pro/                            ← 闭源私有仓（单独推送）
   ├─ crates/aiapp-gen-pro/        真实生成引擎 + Prompt DNA → 闭源
   └─ crates/aiapp-saas/   (规划) 全球运营后台/统计/分成/计费 → 闭源
```

私有仓 `pro` 通过 path/git 复用开源仓公共类型；拆仓后改为 `aiapp-gen = { git = "<开源仓>" }`。

### 4.5 功能分层

| 能力 | 社区版(开源) | Pro(闭源) |
|---|---|---|
| 生成（AI 自动） | ❌ 仅 Mock/模板（`AIAPP_BACKEND=mock`） | ✅ OpenAI 兼容 + 提示词迭代 |
| 企业本地管理后台 | ✅ 管理本企业用户/应用/审核 | ★=（随运行时部署） |
| 全局运营后台 / 分成 / 激励 / 计费 | ❌ | ✅ `aiapp-saas` |
| 官方市场 | 展示已发布应用 | 上传精品/商业化应用 |

### 4.6 核心控制点（护城河）
- **开源钩子**：WASM 运行时、MoonBit 编译器、基础系统库 —— 吸引开发者。
- **闭源命脉**：AI 生成引擎、安全沙箱、分布式调度、应用商店、跨应用协议 —— 缺一处开源内核即"空壳"。
- **生态锁定**：在开源内核上开发的应用，要触达用户必须走闭源分发渠道与 API/SDK。

### 4.7 事件上报契约（为分成/广告/计费预留）
开源侧（企业部署）只产生并**匿名上报**事件到闭源 SaaS：`app_launch`、`app_generate`、`app_report`、`user_register`；闭源侧聚合出统计与分成/计费账单。企业可关闭上报（纯私有），只是不享受分成/云端统计。事件字段用 versioned schema（`docs/telemetry.schema.v1.md`）约定。

### 4.8 分阶段落地
1. **阶段 1（已完成）**：物理分隔——`aiapp-gen` 只留社区能力；真实引擎 + Prompt DNA 移入 `pro/aiapp-gen-pro`；`pro/` 进 `.gitignore`。
2. **阶段 2**：拆 crate 规范化（`aiapp-format`/`aiapp-engine`/`aiapp-host`），`pro/aiapp-saas` 独立成服务。
3. **阶段 3**：定 Telemetry schema，后台统计改为「事件上报 + Pro 聚合」。
4. **阶段 4**：把上报的事件接入计费/分成单元。

## 5. 外部参考资料
- 飞书在线文档（`ai 应用平台`，含完整演进路线/架构图/风险避坑）
  - wiki: https://qcn5zapfb0ro.feishu.cn/wiki/ZpWuwQy8widUSCkDL0Wc9PM2nBd
  - docx: https://qcn5zapfb0ro.feishu.cn/docx/U95ndmSoso8RuoxU706c4QxAnpd

## 文档索引
- `README.md` — 项目简介、流水线、快速运行
- `docs/ARCHITECTURE.md` — 社区版 vs Pro 商业化分层（含拆分动作）
- `pro/`（闭源私有仓）— 真实生成引擎 + 全局运营后台
